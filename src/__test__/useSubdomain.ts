import { describe, expect, it, vi } from 'vitest'

describe('useSubdomain', () => {
  it('getDomain should return the subdomain', () => {
    // TODO: Mock window.location.hostname = 'example.kintone.com'
    // Expect getDomain() to return 'example'
  })

  it('getFullDomain should return the full domain', () => {
    // TODO: Mock window.location.hostname = 'example.kintone.com'
    // Expect getFullDomain() to return 'example.kintone.com'
  })
})
